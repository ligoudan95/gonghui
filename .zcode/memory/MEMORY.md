# MEMORY.md — gonghui 项目跨会话工作记忆

> 每次会话开始由 Team Lead 先读本文件（根 `AGENTS.md`「会话启动检查」）。注意：ZCode 只自动加载根 AGENTS.md，本文件需主动读取。
> 完成阶段性工作后在「近期关键记录」追加（新条目放最上面）；只保留关键事实与决策，过时条目及时清理。
> 团队工作流：Team Lead 规则见根 `AGENTS.md`；角色定义见 `.zcode/agents/`；团队规程与机制对照见 `.zcode/docs/AGENTS_CONFIG.md`；与项目规范冲突时以根 AGENTS.md 为准。

## 环境

- 仓库：https://github.com/ligoudan95/gonghui.git（origin，main 分支）
- 平台：Windows；ZCode 命令行环境 Git Bash（路径含空格须引号）
- 项目：冒险者工会（游戏）——当前处于**策划/规划阶段，尚无代码**
- 防线 hooks（用户级 `~/.zcode/cli/config.json` 注册，脚本实体在 `C:\Users\liangqihang\.zcode\hooks\`，config 已指向该路径——勿删、勿移回工作区）：AskUserQuestion 前后拦截超时特征，支撑「用户未答复=冻结」红线

## 项目速查【随推进回填】

- 总体规划：`新的规划/总体规划.md`
- 文档体系：`新的规划/`（PROJECT_OVERVIEW / SYSTEM_FRAMEWORK / SCRIPT_FRAMEWORK / CODE_STANDARDS 四文档体系尚未建立；建立后由 @docs-updater 登记，并同步 `.zcode/agents/docs-updater.md` 文档映射表与 `architect.md` 阅读优先级）
- 编码规范：未建立（建立后在根 AGENTS.md「项目速查」登记，覆盖 programmer.md 通用模板）
- 程序员自查命令：未建立
- godot-ai MCP：已配置于 `.zcode/config.json`（uvx godot-ai==3.2.4 attach 8000/9500，编辑器桥接）——待建 Godot 工程并安装 `addons/godot_ai` 插件（可从 feitu `addons/godot_ai` 复制）后启用

## 近期关键记录

- 2026-09-21（第四批·五轮复审处置闭环）：v0.5 修订后 5 子代理盲审出 62 项问题（高 1/中 19/低 42，归因三层：落盘残留/记账断裂/新设计边界），用户拍板"A+B 直改+C 拍板后改"。**C 组 7 项拍板**：允许战棋局内撤退（同探索层口径不判重伤）；倒地队员减员出征；地图区域字段改多值；新增单点事件结构；删感知"先手优势"映射；魅力经 #17 权重体系承载；**委托板=接取不即时补位+每周第 1 天日结算周刷新（整批替换·推断默认）+事件授予委托端口**。A 组 11 残留直改+B 组记账修复（**总案修订说明补 v0.5.1/v0.5.2 两节，版本链闭合**；索引决策记录同步）+默认口径约 25 项。docs-updater（agent_4a665e3a）落盘 17 文件+2 收尾，7 项复验全过。变更未提交待用户指示；策划修订两轮闭环，下一步可派 @architect。

- 2026-09-21（第二批·审阅→拍板→修订落盘闭环）：五轮审阅 60 问题经用户逐条拍板（22 方向项 + 7 推断项 + 2 补拍 #23/#24 + 挂单口径"接取不限、进行中限 1"），《修订案 v0.5/v0.5.1》经审核由 @docs-updater 落盘 **18 文件全部完成**（改名 git mv 未提交：09-战旗战斗.md→**09-战棋战斗.md**、00-总案-v0.4.md→**00-总案-v0.5.md**；术语"战旗→战棋/属性鉴定→属性检定/事件专项案→事件文案专项案/美术专项案→视觉专项案"四组全库替换 Grep 零残留；12 项联动复验 + 5 项补正通过）。**核心定稿（不经再次确认不得更改）**：委托-出征解绑（需求人力区间 1-4+超额人数提升奖励；战斗委托同时 1 个进行中；非战斗委托新大类按人力任意并行·完整版存档 DEMO 不做；每名冒险者每日最多一次出征）；撤退=委托失败（同到期口径、进度不保留、无待命切换）；goal_type 四类判据（DEMO 只做清怪+探索）；二级属性 14 项（+命中率/异常状态抗性）+状态两段判定（命中→抗性，增益/地格直接生效）；乘数伤害公式（Σ属性权重×属性值+武器加值）×技能系数、伤害类型归技能定义；幸运大成功降线+兜底；倒地退场不可拉起（仅全灭重伤·固定天数）；设施 DEMO 豁免 2 级；全图可见无迷雾；装备四槽；DEMO 技能不分倾向；自动恢复制；星期必做；公会独立于光辉城；防刷不做靠数值。旧名链条入档：武师(v0.2)→奇书师(v0.3 笔误)→奇术师(v0.4)。变更未提交待用户指示；planner=agent_2dd49f80、docs-updater=agent_481042f6 可 SendMessage 复用；下一步可派 @architect（输入=案 1+案 16+案 9 新名）。

- 2026-09-21（第一批·五轮独立审阅）：系统规划案 18 文档经 5 个全新子代理各从头逐文件独立审阅（互不知晓），整合约 **60 条问题**（高 3 / 中 17 / 低 40）。高危三条（5/5 共识）：①《总体规划.md》工作树删除未提交（**用户拍板：有意删除**，提交时纳入）；②"设施 ≤ 公会等级"硬规则与 DEMO 设施 1-2 级矛盾（→#2 豁免修复）；③事件耗时代价案 2/案 8 相反（→#3 跟案 8 修复）。审阅五子代理为一次性会话不复用。（原误记于 09-20.md 第十五批，已迁移）

- 2026-09-20（第十四批·策划体系定稿+落盘）：**游戏设计案 v0.4 定稿**（四轮迭代：v0.1 草案→16 项选择题拍板→v0.2→v0.3 武师换奇书师删 DLC→v0.4 奇术师更名/双资源纯口径/DEMO 定调剧情暂缓）+ **16 系统规划案四批全部产出并经用户审核**（6 大类：总纲 2/经营 3/冒险 3/战旗 3/内容剧情 2/横向支撑 3；统一六节模板）+ **落盘完成**：`新的规划/DEMO文档/系统规划案/` 18 文件（00-索引+00-总案-v0.4+案 01-16，1737 行），`新的规划/DEMO文档/总体规划.md` 五处修订对齐（用户中途手动重排目录至 DEMO文档/，落盘跟随）。关键定稿决策链与配套拍板（存档=DEMO 最简自动存档）详见当日日志第十四批。**策划阶段完成，下一步派 @architect**（输入=案 1 总纲+案 16 数据总目录为要）；变更未提交待用户指示。策划会话 agent_3d55762d、文档会话 agent_12276995 均可 SendMessage 复用。

- 2026-09-20（第十三批·A机破案+修复0.3.0+**终局闭环**）：**hooks 秒败根因定论**：hooks.json 的 `shell:"bash"` 靠 PATH 裸名解析 bash，而 Windows Git 安装器默认只把 `<Git>\cmd` 加系统 PATH（无 bash.exe），桌面进程 PATH 无 bash → spawn ENOENT 30ms 级秒败——与版本无关（0.16.9/3.14.0 同症）、与 Git 安装位置无关；godot-prompter 同败同理；历批冒烟测试盲区=终端 PATH 含 usr\bin 与桌面进程不同（cmd 环境探针实测 hook 子进程 PATH：有 node 无 bash，终审坐实）。**runner 真本体在 `<安装目录>\resources\glm\zcode.cjs`（非 app.asar）**，asar 解包 Windows 会丢文件须 node 直读。修复 **0.3.0 已终局闭环**（本机 3.14.0 双 hook 实弹零 failed）：process 型 `command:"node"` + args 变量展开（3.14.0 源码证实 process 型 command+args 均展开）。**新坑**：本地 directory 市场添加时复制快照，改源后须移除重加市场才能更新版本；插件更新后必须完整重启（热注册钩表残留旧版会误导排查）。**可复用手法**：hook 环境诊断=cache 副本 hooks.json 换 cmd 内建 `set` dump（PATH 无关探针必达）。另一台机启用：pull → 重加市场 → 装 0.3.0 → 完整重启 → 实弹。

- 2026-09-20（第十二批·插件 hook 执行失败=环境性问题，排查中断存档）：插件 0.2.0（command+bash 范式）已装载并稳定执行——hookCount 1→3，每次 AskUserQuestion 前后各执行一次（来源标签 `plugin.gonghui-defense@dev-gonghui.PreToolUse/PostToolUse.0.0`，标准插件格式）——**但两个 hook 均执行失败**（31-59ms 秒败，日志 `hook.run.failed` 无错误详情字段）。**关键旁证：godot-prompter 的 SessionStart hook 同样失败**（同机同日）→ 判定为本机/本 build（0.16.9）hook 子进程生成的**环境性问题**，非我们写法问题。已排除：node/bash 在 Windows PATH 均可解析（`where node`→C:\Program Files\nodejs\node.exe，`where bash`→C:\Program Files\Git\usr\bin\bash.exe）；已试范式：process+"node"（0.1.0，失败）与 command+bash+`${CLAUDE_PLUGIN_ROOT}`（0.2.0，失败）。**下一步（未做）**：读 ZCode 本体源码定位 hook runner 的 spawn 实现与被吞的错误——应用位于 `C:\Users\liangqihang\AppData\Local\Programs\ZCode\ZCode.exe`（Electron，resources 里找 core.hooks 模块）；若确认平台 bug 则防线维持"纪律层"（AGENTS.md 冻结规则，今天全天实际就是它在扛），插件保留待平台修复。**当前防线状态：机械层仍无效，纪律层独立承担**；插件安装部署步骤（市场添加 `<仓库>/plugins` + 安装）已验证可走通，另一台电脑同步骤即可，但装上后 hook 同样本机环境性失败待解。实验残留 `.zcode/hooks/` 已删（判定实验结论：失败源是插件 hook 而非旧工作区注册）。

- 2026-09-20（第十批·插件化实施）：用户在线拍板「转插件化」后已实施——插件源码 `plugins/gonghui-defense/`（plugin.json：name=gonghui-defense v0.1.0，hooks→./hooks/hooks.json；hooks.json 插件格式外层包装，两 hook matcher `^AskUserQuestion$`，type process，`command:"node"` 走 PATH，`args` 用 `${CLAUDE_PLUGIN_ROOT}/hooks/<脚本>`，脚本与原版逐字节一致）+ 本地 marketplace `plugins/marketplace.json`（name=dev-gonghui，中文显示名「工会防线 Hooks」）；冒烟三场景全过、JSON 全部合法。**方案 A 实施物已清理**（`.zcode/hooks/` 删、工作区 config 回退仅 mcp 无痕、用户级保持 `{}`）。AGENTS.md 防线段与 AGENTS_CONFIG.md（7 处，@docs-updater）已同步插件口径。**分发语义**：每机 clone 后两步——插件市场添加本地目录 `<仓库>/plugins` + 安装「工会防线 Hooks」；插件 hooks 官方文档承诺自动启用（无信任门）；前置=node。**待办**：①用户 UI 安装 → ②实弹验证（触发 AskUserQuestion 查日志 hook 执行记录与 decision；若当前会话未生效则再重启一次后验）——文档错过一次，不盲信。旧 docs-updater agentId（agent_ebddfb57）随重启失效，本批新代理 agent_5f8d4a21。

- 2026-09-20（第十一批·插件安装后首轮实弹）：**插件已执行但失败、且热注册不稳定**——用户 UI 安装成功（cache/dev-gonghui/gonghui-defense，installed_plugins.json scope:user），装后首次提问出现**全天仅有的 2 条 `hook.run.failed`**（Pre+Post 各一，matcher ^AskUserQuestion$，source 标签 `project.workspace-hook-0-*`，durationMs~100，日志无错误详情）＝**runner 确实执行了我们的 hook**（godot-prompter 仅 SessionStart hook 可排除；恢复 `.zcode/hooks/` 脚本再测则连 failed 都无——热注册转瞬即逝，钩表随进程重启才稳定装载）。失败主嫌：process 型 `command:"node"` 在 Windows 直生失败或 `${CLAUDE_PLUGIN_ROOT}` 对 process args 未展开。**已改范式**：仿 godot-prompter（本机唯一实战 hooks 插件）改 `type:"command"` + `command/bash` 双字段内嵌 `${CLAUDE_PLUGIN_ROOT}` + `"shell":"bash"`，版本升 0.2.0（plugin.json + marketplace.json 同步），bash 范式冒烟通过。实验残留：`.zcode/hooks/` 两脚本已放回（无注册，无害，判定实验用；插件验证通过后可删）。**待用户**：插件市场刷新 dev-gonghui → 更新 gonghui-defense 至 0.2.0 → 重启 ZCode → 实弹验证。

- 2026-09-20（第九批·终局判定）：**本平台版本（0.16.9）配置文件 hooks（用户级+工作区）从未被 runner 执行过**——证据链：①全天 16 次启动 `hookCount:0`（含 08:38 用户级注册存活期的启动）；②08:58/09:09 两次 AskUserQuestion 发生在用户级注册存活期，均 6ms 自动裁决（modify/yolo）、无冻结注入、零 hook 执行事件；③二次重启（信任已入库）后实弹提问仍 modify/yolo、零执行。**信任存储真身为 `~/.zcode/security/workspace-hook-trust-v1.json`**（第八批误记 db.sqlite——命中的是我自己会话回声，已更正），gonghui 两条记录（PreToolUse ask_gate + PostToolUse ask_timeout_gate）decision:trusted 且跨重启持久存在，**但 runner 依旧判 pending**——feitu 时代"信任不持久化"的旧记忆方向对（跨重启失效），实况更糟：单次会话内也从未生效。**推论：feitu 时代至今的防线 hooks 在 ZCode 上可能一直是装饰性的**（历代"冒烟测试"都只验脚本本体、从未验 runner 级触发），实际扛住 8-21 事故防线的是 AGENTS.md 的冻结纪律本身。**待用户拍板**：A 转插件化（插件 hooks 文档承诺自动启用、无信任门，用 plugin-creator 技能做本地 marketplace，repo 随库存放，每机 clone 后两步点击安装，`${ZCODE_PLUGIN_ROOT}` 免疫路径——装后仍需实弹验证，文档错过一次不能盲信）/ B 接受现状（hooks 装饰性，防线=AGENTS.md 纪律，等平台修复配置 hooks 再启用）。工作区方案 A 的实施物保留未提交（脚本+注册+文档同步），选 B 则回退注册防混淆。

- 2026-09-20（第八批·重启验证与信任门破解）：**工作区 hooks 信任门根因确认**——重启后日志实锤 `config.project_hooks.pending_trust`（"Project hooks are pending workspace trust and remain blocked"，configScope:project）＝**第二批记忆正确（工作区 hooks 有信任门），官方 diagnosing-hooks 文档"非插件配置 hooks 无信任门"过时，误导了方案 A 评估**（已吸取教训：文档与实机不符时以实机为准）。**信任流程实测**：重启后客户端弹出信任请求（`workspace_hook.review_request_created`），用户 09:15:50 点击信任（`workspace_hook.trust_selected`，requestEnabledCount:2 grantedRecordCount:1，两代各授一条），**授予已持久化进 `~/.zcode/security/workspace-hook-trust-v1.json`**（gonghui 两条记录 decision:trusted；另见 feitu 2026-08-21 同模式历史记录，证明信任门非新增）——**但 09:25 二次重启后 runner 依旧判 pending_trust、hooks 依旧零执行**（详见第九批终局判定）。

- 2026-09-20（第七批·hooks 方案 A 实施）：**用户在线拍板「方案 A：随仓库走」**，已实施——①脚本入仓库 `.zcode/hooks/`（ask_gate/ask_timeout_gate，与原副本逐字节一致，不入 .gitignore）；②注册迁至工作区 `.zcode/config.json`（`hooks.enabled: true`，PreToolUse/PostToolUse process 类型，matcher `^AskUserQuestion$`，`command: "node"` 走 PATH，`args` 用 `${ZCODE_PROJECT_DIR}` 变量）；③用户级 `~/.zcode/cli/config.json` 清空为 `{}` 停用旧注册（原内容已存档于当日日志，可回退）；④AGENTS.md「防线加固」段更正为随仓库口径；⑤AGENTS_CONFIG.md 由 @docs-updater 四处同步（对照表行/目录树补 hooks 分支/第 3 条旧"信任门"表述作废声明/新增第 4 条完整记载）。验证：脚本自仓库位置 PATH node 冒烟三场景全过、双 config JSON 合法。**【重要·实弹验证未通过】本会话实测 hooks 未被 runner 执行**：①当日 ZCode 日志（`~/.zcode/cli/log/zcode-2026-09-20.jsonl`）零条 hook 执行记录，`session_start_hooks`/`user_prompt_hooks` 回合阶段均 0ms 完成（无 hook 可跑）；②本会话一次 AskUserQuestion 超时返回时**无** `[ask_timeout_gate]` 冻结注入（PostToolUse 未触发实锤）；③权限日志显示 yolo 模式下 AskUserQuestion 1-6ms 自动裁决（decision "modify"）——正是 ask_gate 设计要拦的事故特征。**根因主假说：hooks 配置仅在会话启动时加载，本会话进程早于两代注册生效**（用户级今晨迁移、工作区为会话中新增）；备选假说：当前安装版不支持配置文件 hooks（仅插件 hooks）。**下一步（新会话必做）**：重启/新开会话后触发一次 AskUserQuestion 并复查日志——有 hook 执行记录=方案 A 闭环；仍为零=当前版本不支持配置 hooks，防线需**插件化**（插件 hooks 自动启用）才可生效。教训记录：第四批"冒烟测试通过"只验证了脚本本体（echo 载荷直跑 node），从未验证过 runner 级触发——防线此前可能一直未真正生效过。实施按第四批先例（用户直接指令的基础设施修复由 Team Lead 直接执行）未走完整管线。提交：用户拍板暂不提交，变更保留工作区。

- 2026-09-20（第六批·文档修正后复测）：用户修正 hooks 路径记载（`C:\Users\A\`→`C:\Users\liangqihang\`）后的新会话全量复测通过——五类型全部注册成功（无一 not found），tools 白名单"上限限权"维持（实际注入 ⊆ 白名单、无一越权），planner/architect/git-admin 三个受限角色的 Write 拦截在**工具供给层**成立（Write 根本不在注入集，无运行时报错），git-admin Bash 实测只读 `git status` 正常。**Glob/Grep 注入缺失名单扩大**：本次 architect 也未注入 Glob/Grep（第五批仅 planner 到位，其余三个角色缺失）——证实该现象是 ZCode 平台侧注入特性、不按角色稳定复现；因缺失角色均含 Bash 可替代，工作无碍，配置无需改动（AGENTS_CONFIG.md 第 3 条已由 @docs-updater 按此更正）。**【冻结待拍板→已闭环】hooks 跨机兼容方案**：用户提出经 git 在两台电脑间中转工作、用户级绝对路径天然对不上；已评估三方案并呈现——A 随仓库走（脚本入 `.zcode/hooks/` + 工作区 config 注册 + `${ZCODE_PROJECT_DIR}` 变量，推荐）/ B 固定路径+用户级 / C 现状每机适配；AskUserQuestion 超时后按冻结红线冻结，**用户其后在线拍板方案 A 并已实施，见第七批条目**。

- 2026-09-20（第五批·新会话全量实测）：**第四批「待新会话复测」已闭环**——全新会话中五个自定义子代理类型（planner/architect/programmer/docs-updater/git-admin）全部注册成功、实弹派工通过，证实此前 not found 系删库重克隆中间态所致，配置本身有效；general-purpose 回退路径的临时措施解除，回退方案保留作保险。**tools 白名单实测语义为"上限限权"**：各角色实际注入工具 ⊆ frontmatter 白名单、无一越权（git-admin 无 Write/Edit 依旧成立），各角色统一额外内置 RespondToCoordinator（子代理→协调者通信工具，非白名单项、无文件操作能力）。**Glob/Grep 注入不完整的共性现象**：programmer / docs-updater / git-admin 实际未注入 Glob/Grep（planner 正常到位），因三者白名单均含 Bash 可用只读 shell 命令替代，使用无碍；若后续 ZCode 版本修复自动补齐无需改配置。

- 2026-09-20（第四批·hooks 迁移补录）：发现「第二批」的 hooks 迁移记录与实况不符——config 实际仍指向 `D:/ea/feitu/.zcode/hooks/`（跨项目依赖未消除），`C:\Users\A\` 目录在本机不存在。已实际完成迁移：两脚本复制到 `C:\Users\liangqihang\.zcode\hooks\`，用户级 config 两处路径更新，冒烟测试 4 项全过（ask_gate 强制 ask / timeout_gate 特征匹配 / 无特征不输出 / config JSON 合法）；同步修正根 AGENTS.md 与 AGENTS_CONFIG.md 中的错误路径记载。feitu 目录内原副本保留为闲置备份。另：本会话实测 `.zcode/agents` 自定义子代理类型未注册（`Agent type 'planner' not found`），疑为会话中途删库重克隆所致，待新会话复测——期间派工一律走 general-purpose 回退路径，此时 frontmatter `tools:` 硬限权不生效，只读角色仅剩提示词软约束。

- 2026-09-20（第二批）：吸收 feitu `.zcode/` 实战配置三项——①五角色文件升级为自定义子代理类型（frontmatter `readOnly`/`tools` 硬性限权，主代理 `subagent_type: <角色名>` 直接派工，未加载则回退 general-purpose+注入全文；新增红线：子代理内 AskUserQuestion 无效须 Team Lead 转达、拍板管线禁跑 yolo 模式）；②`.zcode/config.json` 配置 godot-ai MCP（待装插件启用）；③防线 hooks 实体迁至 `C:\Users\A\.zcode\hooks\` 并更新用户级 config 路径（两脚本功能测试通过；feitu 目录内原副本转为闲置备份）。
- 2026-09-20（第三批·重启验证）：`.zcode/agents` 自定义子代理类型**已随会话启动加载并生效**（planner / architect 实弹派工成功）。工具限权实测结论：**`tools:` 白名单硬性生效**（architect 工具集中无 Write，写入被工具层拦截）；**`readOnly: true` 单独不生效**（planner 当时无 tools 字段，Write 成功执行了——已修正：planner 补 `tools: Read, Glob, Grep`，下次会话生效）。AGENTS_CONFIG.md 机制对照表已登记该实测结论；派工回退方案（not found 时 general-purpose+注入全文）保留作保险。

- 2026-09-20：建立 ZCode 版 6 角色多 Agent 团队工作流（由 feitu 项目 opencode 版转换）——根 `AGENTS.md`（Team Lead 规则）+ `.zcode/agents/` 五子角色 + `.zcode/memory/` 记忆系统；保留 feitu 沉淀的事故红线（用户未答复=冻结 / 转达必标来源 / 代定不可信）。ZCode 机制映射：Agent 工具派工（注入角色提示词全文）、SendMessage 按 agentId 复用同角色会话、审核闸门=呈现产物后结束回合等用户。

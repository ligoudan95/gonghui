# 案 16《内容数据与配置化规划案》

> 变更记录：2026-09-24 M1 回填——§2.2 补 core 域资源登记表/路径映射表行与批 A/B 新增字段位、总控配置参数组扩注；§2.6 增设实装校验规则登记（DataValidator V-* 体系，含盲审批 2 补强九组）；同日盲审 39 项收尾——S1-1 计数勘正（V-M0-ref-* 七条）/S1-2 开局参数包载体注记（BattleParams 非静态表）/S1-4 新字段（mark_color、ui_tile_fallback_color）及对应校验行扩展；同日解耦复审 49 项收尾——§2.5 补 GameConfig 职责边界（C-5 拍板）、§2.6 补 V-M1-map-layout legend 扩展与 V-B2-cfg-fallback（82 断言位）、新增 §2.7 硬编码豁免登记（豁免 30 项摘要+转登记 5 项）；同日第三轮复审 56 项收尾——§2.6 新增 V-M1-aura-combo（R1-5）与 V-B2-cfg-fallback 断言位勘正（82→75，int 侧死断言删除）、§2.7 A-15 豁免范围收窄（玩家侧已公式化调表）；2026-09-25 M2 事件与检定落地登记——§2.2 事件/委托行更新实装计数（chains 3/nodes 11/options 10/singles 3/hidden_marks 空域/quest 模板 1）+M2 数据定义 10 类注记+naming_registry 113 条、§2.6 新增 V-M2 八组+前缀表六前缀+cfg-domain 加严（luck_floor_y==luck_floor_z_divisor）+挂起规则注记（evp_/tp_/map_id 待 M3）；同日 M2 内部质检 40 项收尾——§2.6 V-M2-ref-graph 扩四新规则（E10①②③④）+V-M2-ref-battle 两变更（先手/分布 token 空串走默认 E11、CHECKIN 载入状态禁控制类 E2-10）。

## 1. 系统定位与目标

- **定位**：全项目数据层统筹规范——16 系统案+5 后续专项案（共 21 案）所有数据表的汇总目录、冲突校验、组织方案与引用完整性规则；DEMO 直接使用（各案建表以本案为总纲）；
- **服务的核心体验**：无直接体验承载；服务内容量产效率与长期维护成本。

## 2. 机制细则

### 2.1 表驱动总原则（入档）

**内容与逻辑分离：调整内容不改程序，扩充内容不动框架**；DEMO 数据结构按完整版标准（范围可小、结构不能歪）。

### 2.2 21 案数据表汇总总目录（本案核心工作）

| 域 | 表名 | 归属案 | 状态 |
|---|---|---|---|
| 总控 | 总控配置（模式开关/系统启用清单/**全局数值参数：调整值换算/难度五档/幸运 X·Y·Z/视野 R/暗门触发半径**+命中/伤害参数（hit_base=0.85 等，17-C23）+二级属性派生/移动力与 AI 怒吼/招募带公式参数（解耦批 B 18 参数）+内容计数带（批 C 10 组）+**UI 视觉参数（ui_tile_fallback_color 地格查无定义兜底色，S1-4——实装 Color(0.32, 0.36, 0.29, 1)）**——实装值清单见 17 案 §3.11 附表） | 案 1/17 | DEMO 必建 |
| 总控 | 资源命名登记表（naming_registry：全库顶层资源 id→域登记，命名信息集中登记——用户拍板新增；双向校验源见 §2.6 V-B2-naming） | 案 16 | DEMO 必建（M0/M1 落地补登记；**全库登记 113 条——M1 末 85→M2 扩至 113（2026-09-25）**） |
| 总控 | 资源路径映射表（assets 域 registry：AssetRegistry，资源 id→res:// 路径集中映射——§2.4「表内不写死路径」原则的承载件；域内白名单仅此一条） | 案 16 | DEMO 必建（M1 落地补登记） |
| 总控 | 总控时间配置、行为耗时表 | 案 2 | DEMO 必建（子集） |
| 公会 | 公会状态、设施定义、设施实例、解锁配置 | 案 3 | DEMO 必建（简化值） |
| 经济 | 物品总表、装备表 | 案 4 | DEMO 必建（货币+初始套装） |
| 经济 | 词条池 / 商店表、配方表、赌箱表 | 案 4 | 先建后空 / 纯存档 |
| 经济 | 掉落表 | 案 4 | 纯存档（不建结构，文档级草案含幸运加成字段位） |
| 冒险者 | 冒险者实例、职业定义表、成长配置表、招募配置表、恢复配置表 | 案 5 | DEMO 必建（职业定义表 M1 新增字段位：sprite_id（单位占位 sprite 引用，批 A）+ mag_pierce_source_attr（法术穿甲换算源表驱动字段、必填 ∈ 七属性，批 C M8——17 案 §3.2 奇术师意志特例的表化承载）） |
| 委托 | 委托模板、委托实例、委托板配置 | 案 6 | DEMO 必建（普通类；goal_type 判据+需求人力区间+超额奖励加成字段，#8/#9；板刷模板 8 个+事件授予 1 个=DEMO 合计 9，P-9，18 案 §2.6）——**M2 落地（拍板：模板只落 1 行）：quest/templates 域 1 张（q_lost_miner_keepsake 事件授予模板）；板刷 8 模板归 M4 委托全套** |
| 地图 | 地图定义（含迷雾配置：启用开关+全亮段/格子集合标记，#25——第八轮盲审补登记）、格子类型、遭遇权重 | 案 7 | DEMO 必建（一张图；目标标记点 6 个=tp_old_well+P-9 新增 5 个） |
| 事件 | 事件链、事件节点、事件选项 | 案 8 | DEMO 必建（2-3 链）——**M2 落地（2026-09-25）：event/chains 3 张（chain_mine_camp/collapse/wisp）+event/nodes 11（evn_*）+event/options 10（opt_*）** |
| 事件 | **单点事件** | 案 8 | DEMO 必建（村子轻事件点用）——**M2 落地：event/singles 3 张（sp_mine_secretdoor/sp_village_cart/sp_village_traveler）** |
| 事件 | 隐藏内容标记 | 案 8 | 先建后空——**M2 落地：event/hidden_marks 域已建、0 行空表（启用清单隔离）** |
| 战斗 | 战棋地图、地格类型、敌人定义、敌方队伍配置、AI 配置、战斗规则配置 | 案 9 | DEMO 必建（2-3 图；M1 新增字段位：地格类型（TileTypeDef）加视觉表驱动字段 fill_color/accent_color/style + description（批 A，替代代码内写死配色）+ **mark_color（S1-4，2026-09-24：动态地格标记色，ENEMY_ENTER_ONCE 陷阱类必填——陷阱落入战场的橙点标记，tile_trap 实装 Color(1, 0.5, 0.1, 0.8)）**；敌人定义加 sprite_id（批 A）；敌方队伍配置加 battle_map_ref（M1 批 1 强制回填，指向战棋地图 id）） |
| 战斗 | 开局参数包定义 | 案 9（定义与默认值；参数清单源自案 8 §2.5） | DEMO 必建（**S1-2 载体注记，2026-09-24**：载体=BattleParams 运行时参数类（RefCounted 纯逻辑类、场景入参聚合，供 BattleSetup.build 消费；先攻三口径/遭遇队伍/出战名单/出生位覆盖/开局载入状态等六维度字段），非静态数据表——DEMO 不建 .tres、不计入资源总数） |
| 战斗 | Boss 脚本表 | 案 21 | 纯存档 |
| 职业 | 技能定义（含 6 条内置普攻；M1 新增 description 字段位——批 A，UI 技能悬停描述消费）、倾向定义 | 案 10 | DEMO 必建（档 1；+敌方技能 4 条（P-10，17 案 §3.8 敌方技能表）+敌方普攻 1 条（17-C16「耗尽转普攻」承接行，17 案 §3.8）；**技能 id 命名规范（D3 落细 2026-09-23）：skl_ 前缀+小写下划线**【占位·架构期可调】——6 条职业普攻=skl_atk_warrior/rogue/mage/priest/ranger/arcanist、敌方通用普攻=skl_atk_enemy_common（权威定义归案 10 §3），其余技能 id 同前缀规则） |
| 职业 | 二转定义 | 案 10 | 纯存档 |
| 状态 | 状态定义、状态实例、互斥组 | 案 11 | DEMO 必建（基础池；含持续时间类型/移除方式预留字段） |
| 世界 | 区域表、场所表 | 案 12 | DEMO 必建（子集行） |
| 世界 | NPC 摆位表 | 案 12 | 先建后空 |
| 剧情 | 主线章节表、结局表、路人台词表、暗线标记表 | 案 13 | 纯存档 |
| 剧情 | 抉择标记表 | 案 19 | 纯存档 |
| 压力 | 压力配置表、怪癖定义表 | 案 14 | 先建后空/纯存档 |
| UI | UI 屏幕清单表、触控规范参数表、可读性参数表 | 案 15 | DEMO 必建 |

**M2 数据定义新增 10 类（2026-09-25 登记；`scripts/data_defs/`，字段位要点）**：event_chain_def（entry_node_id 入口+quest_ref/trigger_point 挂点）/ event_node_def（chain_id+option_ids+outcome）/ event_option_def（kind+check_attr_id+difficulty_tier+success/failure_to 去向+success/failure_outcome+crit/crit_fail_modifier+cost_days）/ event_outcome_def（exit_kind A-D+reward+grant_quest_id+unlock_flag+battle+texts 四档）/ event_modifier_def（text+reward_delta+party_hp_delta）/ single_event_def（检定字段同 option 精简版+perform_resource_id）/ quest_template_def（goal_type/goal_param+人力区间+time_limit_days+reward+acquire_channel）/ reward_def（exp/gold/reputation）/ battle_opening_def（pack_id+first_strike_token（ally_first/enemy_first）+enemy_layout_token（clustered/spread）+initial_status_id+post_battle 战后出口）/ hidden_mark_def（unlock_flag）；配套前缀 chain_/evn_/opt_/sp_/hm_/q_ 入校验前缀表（§2.6）。

### 2.3 冲突与重复校验（修正建议）

1. **职业定义表**：案 5 建立+案 10 补充 → **单表合并**，字段=案 5 全部+案 10 扩展，案 5 主责；
2. **生产耗时**：2026-09-22 用户拍板**设施生产不消耗天数、直接产出**（案 2 口径；原「耗时字段唯一归属案 4 配方表」裁定随耗时字段废除而失效，案 4 配方表已去耗时字段）；案 2 行为耗时表仅管通用行为；
3. **开局参数包**：双处描述 → 定义与默认值**唯一权威=案 9**（参数清单源自案 8 §2.5 六维度）；
4. **委托时限**：唯一归属案 6（确认无冲突）；
5. **状态定义**：案 11 唯一中枢；案 14 复用需扩展**持续时间类型**与**移除方式**两字段（预留，启用前落实）；
6. **掉落表**：唯一归属案 4；
7. **恢复数值**：口径=**基础值（案 5）× 系数（案 3）**，字段命名对齐；
8. **出征耗时唯一归属（照改裁定）**：案 7 地图定义「基础出征耗时」为唯一权威；案 6 委托模板「预估出征耗时」仅展示用；案 2 行为耗时表只留通用行为定义。总出征耗时=基础出征耗时+Σ事件耗时（案 8）（#3）。

### 2.4 数据组织方案（推断，供架构参考）

- **目录按域分组**：`data/core`、`data/guild`、`data/adventurer`、`data/quest`、`data/map`、`data/event`、`data/battle`、`data/status`、`data/economy`、`data/class`（职业/技能/倾向/二转）、`data/world`（区域/场所/NPC 摆位）、`data/stress`（压力/怪癖）、`data/story`（存档）、`data/ui`；
- **资源引用规范**：表内只存资源 id（小写下划线命名），路径映射集中管理一处，禁止表内写死路径；
- **热重载方向**：开发期配置热重载（供架构参考的策划需求）。

### 2.5 DEMO/完整版模式开关

总控配置（对齐案 1）：模式标记 + 系统启用清单（暂缓系统整块关闭）；各系统启动时读清单决定挂载。

**GameConfig 职责边界（C-5 拍板·用户拍板 2026-09-24）**：GameConfig 单例**专用 = 系统启停 / 难度档 / 版本标签**三职；数值参数的**合法直读口径 = `get_record(CoreConfig.CFG_MAIN_ID)` 取 CoreConfig 实例后按字段直读、经参数注入使用**（CFG_MAIN_ID=&"cfg_main" 单源声明，铁律④跨层走公开接口的既定口径延续）——数值读取不新增 GameConfig 转发面。

### 2.6 数据校验方向（引用完整性检查清单，推断）

| 链路 | 检查项 |
|---|---|
| 委托→事件链→节点→选项 | 引用存在；去向节点存在或出口合法 |
| 选项→出口 | 参数包字段合法（六维度枚举）；奖励引用存在；D 类出口 DEMO 模式被启用清单拦截 |
| 委托→地图 | 目标地图 id/目标点/采集点存在且与 goal_type 参数匹配；需求人力区间合法（1 ≤ 下限 ≤ 上限 ≤ 4）；编队人数 ∈ 该委托区间 |
| 地图交互点→事件 | 事件链 id 或单点事件 id 存在（村子轻事件=单点事件） |
| 迷雾配置→地图格子 | 迷雾全亮段 ⊆ 地图格子集合（#25，案 7 §3 地图定义迷雾字段——第八轮盲审补） |
| 事件授予委托→委托模板 | 委托模板 id 存在（C8 端口；DEMO 演示 1 处需校验） |
| 事件金校验口径（P-11） | 事件金校验按**基础值**校验（单出口基础金 ∈ 配置域 [5,30]）；修饰性叠加（如 E3a 30+20=50 金的选项叠加）不计入域校验；0 金合法（字段缺省=无货币出口）；经验 0 合法（字段缺省=无经验出口，纯选择型出口适用） |
| 委托→目标点（P-9） | 板刷模板 explore 判据的 tp_mine_* 目标点 id 在案 7 目标标记点列表中存在且匹配（5 点全登记） |
| 委托→敌方配置（P-9） | 板刷模板 clear 判据的 enc_m1_lair_pack 敌方配置 id 在案 9 敌方队伍配置表（含 18 案 §4.1 增补行）中存在（第七轮表述勘正：17 案 §3.8 不含 enc_* id、仅参数引用） |
| 事件 B 出口→敌方配置 | B 类出口的敌方配置 id（enc_m1_wisp_nest）在案 9 敌方队伍配置表（含 18 案 §4.1 增补行）中存在（18 案 §4.1 登记） |
| 遭遇权重→敌方配置 | 随机遭遇配置 id（enc_m1_random_pack）在案 9 敌方队伍配置表（含 18 案 §4.1 增补行）中存在（案 7 遭遇权重表引用，18 案 §4.1 登记） |
| goal_type 参数完整性 | 清怪=敌方配置引用/探索=目标点 id/收集=物品 id 与数量/护送=护送终点 id（案 6） |
| 委托类型/执行大类枚举 | 执行大类枚举（战斗/非战斗）为独立字段，与类型枚举分开校验；非战斗（存档）——DEMO 模式被启用清单拦截（#24） |
| 地图/委托→区域 | 区域 id（或列表各项）存在于案 12 区域表（唯一权威） |
| 区域-地图映射 | 单向：仅地图侧持有（案 7），区域侧列表为派生查询不落盘（P3） |
| 技能→状态/地格 | 状态 id 存在且来源允许；地格引用案 9 格子类型 |
| 技能→属性权重表 | 属性 id 合法、权重非负（权重和=1.0±0.01——17 案 §3.4 定值回填，两案相互指向断链闭合·第七轮；原「归数值专项案」占位指向废除） |
| 状态施加链 | 两段判定读数属性存在（命中率/异常状态抗性，案 5） |
| 状态→互斥组 | 组 id 存在；组内无同名 |
| 敌人→技能/掉落 | 引用存在；掉落物品 id 存在（案 4）（DEMO：掉落表纯存档，掉落引用字段允许空值） |
| 敌人→种族/系别标签 | 枚举合法 |
| 敌人 AI 层级 | AI 层级枚举合法（L1-L4，案 21） |
| Boss 脚本→技能/召唤/状态 | 引用存在（案 21） |
| 免疫组→互斥组/大类 | 组 id/大类 id 存在（案 21） |
| 配方/装备→物品/词条 | 原料与产出 id 存在 |
| 装备→槽位 | 四槽枚举合法（案 4） |
| 倾向→技能池 | 引用存在且档位合法 |
| 冒险者→职业/技能/装备 | 引用存在；资源类型与职业法系/物理口径一致 |
| 委托→获取渠道 | 事件授予专用模板不入周刷新生成池（案 6） |
| 事件奖励数值域 | 经验 ∈ [10,20]、金币 ∈ [5,30]、声望展示 ≤2、事件耗时 ∈ {0,1}（案 18） |

**实装校验规则登记（DataValidator，V-* 编号 = 代码报错码；2026-09-24 M1 回填——M0 批 1-3 / M1 批 1 / 解耦批 A / 盲审批 2 四期累计，含盲审批 2 补强九组）**：

| 编号 | 检查项 |
|---|---|
| V-M0-load | 装载问题转译：GameData 扫描层 issues（重复 id/类型域不匹配/坏 .tres）→ error（盲审批 2 A-9②——坏文件单独跑校验不再绿灯）；assets 域白名单（仅 registry 一条，A-9①） |
| V-M0-id-required / id-filename / id-prefix | id 与 display_name 必填；文件名 stem == 资源 id；域前缀规范 |
| V-M0-enum / num-domain / count | 枚举值域（来源/叠加/移除 token 等）；资源消耗/射程/移动力值域（上限读 cfg move_base_cap）；全库计数带（带值读 cfg content_* 组） |
| V-M0-ref-* 七条 | 技能→状态/属性权重（和=1.0±0.01）/owner、职业普攻位、敌人技能、队伍条目、互斥组引用闭合（skill-status/skill-attr/skill-owner/class-atk/enemy-skill/pack/mutex 七个检查——S1-1 计数勘正，2026-09-24） |
| V-M1-* 六条 + V-M1-ref-asset | 地格状态绑定/地图布局（**解耦复审扩展：legend 必含 '.' 图例项**——空格地格未定义不再静默回退）/出生位（界内·非障碍·联合查重）/技能地格引用/战斗域计数；**V-M1-ref-pack-map**：battle_map_ref 非空强制且可解析，**批 2 A-3 加严：队伍条目 count_max 总和 ≤ 引用地图 enemy_spawns 数——防 battle_setup 静默截断吞兵（队伍容量防超编）**；V-M1-ref-asset：AssetRegistry path res:// 前缀且文件存在 |
| V-M1-aura-combo（第三轮复审 R1-5） | AURA_3X3 技能组合合法性：damage_type 必须 NONE（DEMO 怒吼无伤害段；伤害型光环待需求出现时松绑）且 effects ⊆ STATUS_APPLY（HEAL/TILE_SPAWN/COMBAT_MOD 对光环无消费通路） |
| V-B2-cfg-fallback（解耦复审 C-3 护栏；断言位第三轮复审勘正） | **全部代码兜底常量 == cfg_main 对应字段值一致性断言（75 断言位：int 15+float 14+color 35+font 9+完备性 2（version_label 非空/演出延时正数）——原登记 82 系含 int 侧恒真死断言的旧值，死断言已删）**——表值调整而兜底漏改（cfg 未注入路径行为分叉）在落盘校验侧即拦截；跨层口径特例：校验器引用 battle/adventurer/ui 层兜底常量做断言（校验器性质=跨层一致性工具，被引类不反向依赖、无环） |
| V-A-tile-visual / sprite-id / mod-keys / class-attr（解耦批 A/C） | 地格视觉表驱动字段必填（fill_color 回填校验；style=RAISED/BLOCK 时 accent_color 必填；**ENEMY_ENTER_ONCE 触发类 mark_color 必填——S1-4 扩展，2026-09-24**）；职业/敌人 sprite_id 非空且 AssetRegistry 有登记；状态/技能修正键 ∈ 合法集；职业 mag_pierce_source_attr 必填且 ∈ 七属性 |
| V-B2-eq-class（批 2 A-1） | 装备引用：class_ref 存在且为职业表（error）+ 每职业至多一条初始装备引用（唯一性 error——原断链仅运行时静默按零装备装配） |
| V-B2-naming（A-2） | 命名登记表双向强制：全库顶层资源 id 必须有登记条目且 domain 与实际域一致；条目查重；spr_ 条目须在 AssetRegistry mapping、tend_ 条目须存在于职业表 tendencies；登记不存在资源=多登记拦截、漏登记拦截 |
| V-B2-status-src（A-4） | 状态来源双向闭合：技能 STATUS_APPLY 引用的状态 allowed_sources 必含 SKILL（error）；allowed_sources 含 SKILL 但零技能引用 → 孤儿 warning（白名单豁免位预留、当前空集） |
| V-B2-owner（A-5） | owner 双向闭合：普攻 owner == 引用职业（error）；owner 为敌人的技能须被其 skill_ids/common 回含（error）；owner 为职业的 tier=0 技能未被任何普攻位引用 → 不可达 warning |
| V-M0-cfg-domain（A-6 加严） | cfg 值域：三个除数 > 0（**防除零产 NaN**）、命中钳制带有序且 ∈ [0,1]、难度五档键集恰合、启用清单键域合法且全覆盖、批 B/M 公式参数基本值域（正数/率值 (0,1]/计数带 min ≤ max） |
| V-B2-value-domain（A-7） | 值域盲位：职业 attr_ranges 每键下限 ≤ 上限；敌人 role_tag ∈ 合法集；状态 default_duration ≥ 0；**cfg ui_tile_fallback_color 回填校验（alpha ≤ 0 拦截）——S1-4 扩展，2026-09-24** |
| V-B2-tile-matrix（A-8） | 地格三元联动矩阵：NORMAL/OBSTACLE 必空 status_id、STATUS+STANDING 必带、STATUS+ENEMY_ENTER_ONCE 允许空（纯伤害陷阱口径）；TILE_SPAWN 技能效果 dot_attr_id ∈ 七属性（damage_expr 结构化拆字段 dot_attr_id/dot_coefficient 后的引用校验——伤害表达式不再走自由字符串） |
| V-M2-ref-graph（M2 事件图；质检 40 项 E10 四新规则扩入，2026-09-25） | 事件图引用闭合：选项去向节点存在；入口节点 chain_id 与本链一致；检定选项双去向非空（失败也推进硬标准）+纯选择不允许失败去向；*_to 与 *_outcome 互斥；成功去向双空=死路拦截；节点 option_ids 回链；**①去向节点须与挂载节点同链 chain_id 一致（E10①——跨链去向=链图越界）**；**②孤儿选项（无任何节点挂载）报错（E10②——不可达）**；**③检定链可达节点 outcome 对应侧 crit 档强制非空（E10③——CHECK 选项 success_to 命中侧 crit_success / failure_to 命中侧 crit_failure 可达才强制，纯选择去向无档位语义不计；数据侧死键同步清理见案 18 §4.5）**；**④节点 outcome 与 option_ids 互斥禁止混排（E10④——终端节点不带选项）** |
| V-M2-ref-exit（M2 出口） | 出口类型值域：C/D 计数 == 0（DEMO 零实例口径——拦截通路留引擎不留数据）；B 出口必带 battle.pack_id 与 battle.post_battle（战后出口） |
| V-M2-ref-battle（M2 B 出口战斗；质检 40 项两变更，2026-09-25） | B 出口战斗引用：pack_id ∈ enemy_packs；**先手/分布 token 空串合法走默认、非空查值域（E11 放宽——first_strike_token ∈ {ally_first, enemy_first}、enemy_layout_token ∈ {clustered, spread}，空=NORMAL 先手/默认分布，对齐案 8 §2.5「未填维度走默认值」）**；initial_status_id ∈ status/stats 且 allowed_sources 含 CHECKIN（检定带入层级，案 11）**且禁控制类状态（E2-10——定身/蛊惑经 CHECKIN 载入因 from_next_turn_only + duration 1 完全空转，配置层拦截）** |
| V-M2-ref-quest（M2 授予委托） | 授予委托引用：grant_quest_id ∈ quest/templates 且模板 acquire_channel == EVENT_GRANT（事件授予专用口径 18-C3，不入周刷新池） |
| V-M2-num-domain（M2 数值域） | 事件数值域：exp ∈ [10,20] / gold ∈ [5,30]（基础值口径，P-11）/ reputation 展示 ≤2 / cost_days ∈ {0,1}——选项与单点事件双查（案 18/17 案 §3.11 #9 对应） |
| V-M2-four-texts（M2 四档文本） | 四档文本：success/failure 非空（全出口）；链内出口 crit 档严格必填（单点事件宽口径）——「失败也推进」文案验收的落盘侧保障（案 18 §3.3） |
| V-M2-counts（M2 计数带） | 事件域计数带（读 cfg content_event_chains/singles [3,3] 恒定断言——M2 加内容改表即可） |
| 前缀表新增（M2） | chain_ / evn_ / opt_ / sp_ / hm_ / q_ 六前缀入域前缀规范（V-M0-id-prefix 消费） |
| V-M0-cfg-domain（M2 加严） | luck_floor_y 必须 == luck_floor_z_divisor（幸运兜底两参数同值锚定——漂移即报错；Y=4/Z 除数 4 现值满足） |
| 挂起规则注记（M2） | 事件数据中的 evp_（出口交付交互点）/ tp_（目标点）/ map_id（地图引用）引用校验**挂起待 M3 地图域落地后自动生效**（M2 建表不校验，防误报） |

### 2.7 硬编码豁免登记（解耦复审 2026-09-24）

**登记口径**（复审三判据，后续同类项按此归口）：**改数会变结算 → 入表**（cfg_main / 域表字段）；**加内容会碰 match 分支 → 表字段化**；**同一口径出现第二处 → 单源化**（常量类/单源模块）；三不沾者豁免（行为语义/语言级常量/引擎配置面）。豁免≠免检：豁免项因数值变更引发结算分歧时，须回本节重审归口。

**豁免 30 项摘要**（三席复审认定，代码常量保留合法；下表 22 条为合并同类后的摘要条目，覆盖复审 30 项）：

| # | 豁免项 | 口径理由 |
|---|---|---|
| 1 | MAX_ROUNDS=50 | 死循环护栏（到达即判失败兜底），非平衡数值 |
| 2 | MAX_REROLL=1000 | 招募整组重掷护栏，非平衡数值 |
| 3 | 语言级常量（0/1/-1/算法常量/量纲换算 0.01） | 语言与量纲常量，入表无意义 |
| 4 | 效果种类 match 分支（EffectKind 等） | 行为代码化——分支=效果种类语义，参数已全字段化 |
| 5 | AI 四级策略序（L1 优先级链） | 纯行为序无权重阈值可调（怒吼门槛已入表 ai_roar_ally_count_line） |
| 6 | AURA 切比雪夫形态语义 | 3×3 形态=机制语义非数值（半径参数已入表） |
| 7 | unit_builder._RaceTagOf 有限枚举映射 | 有限枚举查表，已与 RACE_MULT_KEYS 收口协同 |
| 8 | tscn 布局 offset | 布局常量属场景文件本职（铁律①管代码零硬编码，不管场景布局） |
| 9 | tscn 底色三场景单源 | 同上，场景侧单源已收口 |
| 10 | preload scene_manager 三处 | 机制声明（场景注册表），非数据 |
| 11 | gen_unit_sprites 程序化美术参数 | 临时占位美术工具，M6 退役随删 |
| 12 | SYSTEM_KEYS 22 键 | 启用清单键域本体；V-M0-cfg-domain 双向校验兜底（表侧多键/缺键均拦） |
| 13 | 难度档名校验断言本体 | 校验不变量（五档键集恰合的断言字面量） |
| 14 | project.godot 引擎配置面 | 引擎配置不属游戏数据层 |
| 15 | 校验结构不变量（权重和 1.0±0.01 / 容量约束等） | 校验规则本体即常量 |
| 16 | 按钮尺寸 tscn 单源 | 场景侧单源（触控热区规格归案 15 参数表管设计值） |
| 17 | 呼吸闪烁周期 const 集中 | 演出节拍常量集中单源（一处改全生效） |
| 18 | 日志上限 500 | 工程护栏（内存界），非玩法数值 |
| 19 | CELL_SIZE 钳制带 | 结构不变量（栅格渲染尺寸合法域断言） |
| 20 | 路径预览插值近似 | 渲染近似算法常量 |
| 21 | 边框条宽/图标尺寸 const | UI 结构常量集中单源（视觉数值已入表的除外） |
| 22 | 演出 delay @export 注入口 | 测试注入口机制本体（表侧权威值 ui_battle_delay_seconds 已入表） |

**转登记 5 项**（不豁免、留痕跟踪）：

| 编号 | 项 | 归宿 |
|---|---|---|
| A-11 | 视线豁免阈值 range>1 | 完整版 SkillDef.ignores_los 字段预留（远攻视线规则启用时字段化） |
| A-15 | yardstick 测试敌参数字面量 | 金标准性质保留知悉（§3.8 验算锚点，改案须同步改测试）；**第三轮复审豁免范围收窄**：玩家侧输入（穿甲/暴击）已改公式化调表（calc_phys_pierce/crit_rate 读 cfg），豁免现仅覆盖敌方纸面参数（护甲/闪避字面量——验算基准设定值） |
| A-17 | source_id &"battle_init" | 日志语义标识（开局载入状态来源名，语义非数值） |
| B-13 | result_panel 占位文案 3 组 | M2 待办（正式结算面板文案入表时收口） |
| B-22 | 调试块拆除清单 | M2 待办（含 DebugWarnLabel 已拆、SaveWarnLabel 预留） |

## 3. 数据与配置需求

本案自身即数据总纲（2.2 为唯一权威清单）；新增数据表须经本案登记。

## 4. 与其他系统的接口

- **输入**：全部其余 20 案的数据需求节；**输出**：总目录与校验规则→架构师（程序方案阶段直接输入）；模式开关→全部系统启动流程；
- **依赖**：全部其余 20 案；**被依赖**：全部系统案、后续专项案。

## 5. DEMO 口径

- **做**：建全部「DEMO 必建」表（子集数据）与「先建后空」表结构；目录与引用规范落地；模式开关；引用完整性检查跑通委托→事件→战斗全链路；
- **不做（暂缓）**：纯存档表不建（仅文档级草案）；热重载视架构成本裁剪（最低要求重启生效）。

## 6. 风险与待专项细化项

- **风险**：① 表结构中期变更的连锁返工（2.3/2.6 是防线，架构阶段落为自动检查）；② 「先建后空」表被误当已实现（启用清单严格隔离）；③ 资源 id 命名混乱；
- **待专项细化**：架构师对组织方案/热重载/校验的可行性裁定；数值与事件文案专项案按本案目录填充。
